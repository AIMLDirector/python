from datetime import datetime
from datetime import date
import random

print(datetime.now())
print(dir(datetime))
print(dir(random))

dayTime = date.today()
print("Day:", (dayTime.year, dayTime.month, dayTime.day))\

DateTime = datetime.now()
print(DateTime.date())
print(DateTime.year)
print(DateTime.timestamp())
print(DateTime.month)
print(DateTime.minute)

file1.DateTime

 # logging mechanicsm :    

class code
 function code
     loop code















